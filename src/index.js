import t from './template/t'
import ast from './template/ast'
import Component from './vdom/component'
import render, { renderVDOM } from './render'
import vdom from './vdom/vdom'

export default {
  t,
  ast,
  Component,
  render,
  renderVDOM,
  vdom
}
